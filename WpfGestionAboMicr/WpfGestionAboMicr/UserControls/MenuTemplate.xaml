<UserControl x:Class="WpfGestionAboMicr.UserControls.MenuTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="250"
             x:Name="Root">
    <Grid Background="#02192e">
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" BorderBrush="Black" BorderThickness="0,0,0,1">
            <TextBlock Text="{Binding HeaderMenu, ElementName=Root}" Margin="15,0,0,0"
                       Foreground="White" VerticalAlignment="Center"/>
        </Border>

        <DockPanel Grid.Row="1" x:Name="ItemsHost" LastChildFill="False"/>

        <!-- Footer -->
        <Border Grid.Row="2" BorderBrush="Black" BorderThickness="0,1,0,0">
            <TextBlock Margin="15,0,15,0" TextAlignment="Center"
                       Foreground="White" VerticalAlignment="Center">
                <Run FontWeight="ExtraLight" Text="{Binding Version, ElementName=Root}"/>
                <Run>◦</Run>
                <Run FontWeight="DemiBold" Text="{Binding AppName, ElementName=Root}"/>
            </TextBlock>
        </Border>
    </Grid>
</UserControl>